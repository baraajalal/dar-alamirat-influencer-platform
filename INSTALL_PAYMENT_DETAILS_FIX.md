# إصلاح تفاصيل المستحق وقيمة المسودة

## سبب المشكلة

أصبح الحقل `payments.expected_amount` يحمل قيمة افتراضية `0`. وعند إنشاء تكليف جديد كان النظام يحفظ قيمة المقابل في `payments.amount` و`assignment_compensations.amount`، بينما تبقى `expected_amount = 0`.

الواجهة كانت تعتبر الصفر قيمة نهائية، لذلك ظهر المستحق بقيمة صفر رغم وجود قيمة المقابل.

## ما يصلحه التحديث

- إظهار تفاصيل المقابل في حالة المسودة وجميع الحالات الأخرى.
- قراءة قيمة المستحق بالترتيب من:
  1. `payments.expected_amount` إذا كانت أكبر من صفر.
  2. `payments.amount`.
  3. `assignment_compensations.amount`.
- إظهار نوع المقابل وقيمته وتوقيت الدفع وموعده وملاحظاته.
- إصلاح بطاقات إجمالي المستحق والمتبقي في قائمة المدفوعات.
- إصلاح السجلات الحالية التي تحمل `expected_amount = 0`.
- مزامنة قيمة المستحق تلقائيًا في أي سجلات جديدة.

## التركيب

1. انسخ مجلدي `app` و`supabase` إلى جذر المشروع ووافق على الاستبدال.
2. شغّل الملف التالي داخل Supabase SQL Editor:

```text
supabase/migrations/202608010014_payment_amount_sync.sql
```

3. شغّل:

```cmd
rmdir /s /q .next
npm run typecheck
npx eslint . --quiet
npm run dev
```

## التحقق

افتح:

```text
/dashboard/payments
```

يجب أن تظهر قيمة المسودة في إجمالي المستحق والمتبقي.

ثم افتح تفاصيل أي مسودة. يجب أن تظهر:

- نوع المقابل.
- قيمة المقابل.
- نوع التعاون.
- توقيت الدفع.
- موعد الدفع المتوقع.
- بيانات الطلب أو القسيمة أو المنتجات عند وجودها.

حالة المسودة تمنع التنفيذ المالي فقط، ولا تخفي تفاصيل المستحق.
