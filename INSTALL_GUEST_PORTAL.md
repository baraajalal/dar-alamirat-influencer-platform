# بوابة المؤثر الضيف للمحتوى والنشر — V1

## المتطلبات السابقة

- تطبيق تحديثات قاعدة البيانات حتى:
  - `202607250009_payments_management.sql`
- وجود المتغيرات التالية في `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

> مفتاح `SUPABASE_SERVICE_ROLE_KEY` يبقى في الخادم فقط، ولا يوضع في متغير يبدأ بـ `NEXT_PUBLIC_`.

## التركيب

1. فك محتويات ملف التحديث داخل جذر المشروع:

```text
C:\Users\DAR ALAMIRAT\influencer-system-clean-baseline
```

2. وافق على دمج المجلدات واستبدال الملف التالي فقط عند الطلب:

```text
app/dashboard/campaigns/[id]/page.tsx
```

3. افتح Supabase ثم:

```text
SQL Editor → New query
```

4. شغّل الملف كاملًا:

```text
supabase/migrations/202607260010_guest_content_portal.sql
```

النتيجة المتوقعة:

```text
Success. No rows returned
```

5. أعد تشغيل المشروع:

```cmd
rmdir /s /q .next
npm run typecheck
npx eslint . --quiet
npm run dev
```

## اختبار الدورة الكاملة

1. افتح حملة تحتوي على مؤثر مرتبط.
2. اضغط **إدارة المحتوى والرابط** من بطاقة المؤثر.
3. اضغط **إنشاء رابط المؤثر** وحدد مدة الصلاحية.
4. انسخ الرابط أو أرسله عبر واتساب.
5. افتح الرابط في نافذة خاصة.
6. أدخل آخر 4 أرقام من جوال المؤثر المسجل.
7. ارفع مسودة أو أضف رابط Google Drive.
8. من لوحة الموظف اعتمد المسودة أو اطلب تعديلًا.
9. عند طلب تعديل، ارفع نسخة جديدة من نفس رابط الضيف.
10. بعد اعتماد المحتوى، أضف رابط النشر وإثبات النشر اختياريًا.
11. من لوحة الموظف اعتمد رابط النشر.
12. عند الدفع بعد النشر، لا يسمح بإرسال المستحق للمالية إلا بعد اعتماد نشر جميع عناصر المحتوى المطلوبة.

## ما يضيفه التحديث

- رابط ضيف سري لكل تكليف، بدون إنشاء حساب للمؤثر.
- تحقق بآخر 4 أرقام من الجوال.
- انتهاء صلاحية، إلغاء الرابط، حد للمحاولات الخاطئة وقفل تلقائي.
- رفع صور وفيديو وPDF حتى 50MB، أو استخدام رابط خارجي.
- حفظ جميع نسخ المحتوى والتعديلات دون حذف النسخ السابقة.
- اعتماد، طلب تعديل أو رفض من الموظف المخول.
- رفع رابط النشر مع إثبات صورة/PDF.
- اعتماد رابط النشر وربطه تلقائيًا بشرط الدفع بعد النشر.
- مساحات تخزين خاصة:
  - `campaign-content`
  - `publication-proofs`
- سجل نشاط لعمليات إنشاء الرابط، رفع المحتوى، المراجعة واعتماد النشر.

## المسارات الجديدة

```text
/portal/assignments/[token]
/dashboard/campaigns/[id]/influencers/[assignmentId]
```

## ملاحظات أمنية

- قاعدة البيانات لا تحفظ رابط الضيف الخام؛ تحفظ SHA-256 فقط.
- الرمز الخام يظهر للموظف مرة واحدة عند الإنشاء.
- بيانات البنك والهوية والملاحظات الداخلية لا ترسل إلى بوابة الضيف.
- الملفات في Storage خاصة، وتعرض بروابط مؤقتة.
- عمليات المراجعة الحساسة تنفذ في الخادم بعد التحقق من صلاحية الموظف.
