# دار الأميرات — تسجيل المؤثر وتأكيد بيانات البنك V1

## ما يضيفه التحديث

- بعد رفع رابط النشر من بوابة الضيف، يظهر تنبيه إلزامي لاستكمال الدفع عبر حساب المؤثر.
- المؤثر المسجل يدخل مباشرة من نفس الشاشة.
- المؤثر غير المسجل ينتقل إلى طلب تفعيل حساب مستخدم.
- بعد تسجيل الدخول تظهر بيانات البنك بشكل مقنّع فقط.
- يستطيع المؤثر تأكيد البيانات الحالية أو إرسال طلب بيانات جديدة.
- التحديث الجديد لا يستبدل البيانات الحالية قبل اعتماد المالية.
- صفحة للمالية لمراجعة التأكيدات وطلبات التحديث:
  `/dashboard/finance/bank-profiles`
- التحويل البنكي لا ينتقل إلى الاعتماد أو التنفيذ قبل:
  1. وجود حساب مؤثر مربوط بالملف.
  2. تأكيد المؤثر لبيانات البنك.
  3. اعتماد المالية للبيانات.
- القسائم والمنتجات لا تتأثر بهذا الشرط.

## حماية البيانات

- المتصفح لا يستلم الآيبان أو الهوية كاملين عند عرض المؤثر.
- واجهة المؤثر تعرض قيمًا مقنّعة فقط.
- الوصول المباشر إلى جدول البيانات المالية أصبح للـ Admin والمالية فقط عبر RLS.
- طلبات التحديث الكاملة لا يستطيع المؤثر قراءتها مباشرة من قاعدة البيانات؛ يتم التعامل معها عبر Server API.
- ملفات شهادة الآيبان تحفظ في Bucket خاص باسم `bank-certificates`.

> هذا التحديث يطبق الإخفاء المقنّع وصلاحيات الوصول الصارمة. التخزين يعتمد حماية وتشفير Supabase للبنية وقاعدة البيانات. التشفير الحقلي بمفتاح تطبيق مستقل ليس جزءًا من هذه النسخة.

## التركيب

1. انسخ محتويات ZIP إلى جذر المشروع ووافق على دمج واستبدال الملفات.
2. شغّل الملف التالي في Supabase SQL Editor:

```text
supabase/migrations/202607260011_influencer_bank_confirmation.sql
```

3. تأكد من وجود:

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

4. امسح كاش Next وشغّل الفحوصات:

```cmd
rmdir /s /q .next
npm run typecheck
npx eslint . --quiet
npm run dev
```

## الاختبار

### مؤثر غير مسجل

1. اربطه بحملة تحتوي على تحويل بنكي.
2. أنشئ رابط ضيف.
3. ارفع المحتوى ثم رابط النشر.
4. يظهر تنبيه «تسجيل الدخول أو طلب حساب».
5. افتح «طلب تفعيل حساب مؤثر» وأكمل طلب التفعيل.
6. بعد اعتماد الحساب سجّل الدخول من نفس شاشة الاستكمال.
7. أضف بيانات البنك من حساب المؤثر.
8. اعتمدها من صفحة المالية.
9. جرّب إرسال المستحق للمراجعة.

### مؤثر مسجل

1. ارفع رابط النشر من رابط الضيف.
2. اضغط «تسجيل الدخول والتأكيد».
3. سجّل الدخول بحسابه.
4. تظهر بيانات البنك مقنّعة.
5. اضغط «البيانات صحيحة»، أو «تحديث بيانات البنك».
6. تعتمد المالية البيانات من `/dashboard/finance/bank-profiles`.

## رسائل المدفوعات الجديدة

- `INFLUENCER_ACCOUNT_REQUIRED`: يجب تفعيل حساب المؤثر أولًا.
- `BANK_PROFILE_REQUIRED`: بيانات البنك لم تعتمدها المالية بعد.
