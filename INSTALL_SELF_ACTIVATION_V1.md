# دار الأميرات — التفعيل الذاتي للمؤثر عبر البريد V1

هذا التحديث يلغي اعتماد المنسق اليدوي لإنشاء حساب المؤثر عندما يبدأ التفعيل من رابط تكليف ضيف موثّق.

## المسار المنفذ

1. المؤثر يرفع رابط النشر من رابط الضيف.
2. عند وجود تحويل بنكي يظهر زر **تفعيل الحساب عبر البريد**.
3. يكتب المؤثر البريد وتأكيده وآخر 4 أرقام من الجوال المرتبط بالحملة.
4. النظام يطابق الجوال ثم يرسل OTP من 6 أرقام إلى البريد.
5. بعد التحقق يُنشأ أو يُربط حساب Supabase Auth بملف المؤثر.
6. يطلب النظام إنشاء كلمة مرور جديدة خاصة بالمؤثر.
7. ينتقل المؤثر إلى بيانات البنك المقنّعة لإضافتها أو تأكيدها أو تحديثها.
8. بعد ذلك يستطيع فتح لوحة المؤثر والحملات والمستحقات وملف الأعمال والأداء.

لا يتم إرسال كلمة مرور دائمة عبر البريد. رمز البريد يستخدم مرة واحدة فقط، ثم ينشئ المؤثر كلمة مرور خاصة لا يراها الموظفون.

## الملفات والمسارات الجديدة

- `/portal/activate-account`
- `/portal/set-password`
- `/portal/dashboard`
- `/portal/campaigns`
- `/portal/portfolio`
- `/portal/performance`
- `/portal/payments`
- `/portal/profile`
- `/api/influencer-activation/prepare`
- `/api/influencer-activation/finalize`
- `/api/influencer-activation/password-complete`

## التركيب

فك ضغط ملف التحديث داخل جذر المشروع:

```text
C:\Users\DAR ALAMIRAT\influencer-system-clean-baseline
```

وافق على دمج المجلدات واستبدال الملفات الموجودة.

## قاعدة البيانات

شغّل هذا الملف بعد migrations رقم 010 و011:

```text
supabase/migrations/202607300012_influencer_self_activation_dashboard.sql
```

من:

```text
Supabase → SQL Editor → New query
```

النتيجة المتوقعة:

```text
Success. No rows returned
```

## إعداد OTP في Supabase

من:

```text
Supabase → Authentication → Email Templates → Magic Link
```

اجعل القالب يعرض رمز OTP بدل الاكتفاء برابط Magic Link. مثال بسيط:

```html
<h2>رمز تفعيل حسابك في منصة دار الأميرات</h2>
<p style="font-size:28px;font-weight:700;letter-spacing:6px">{{ .Token }}</p>
<p>الرمز صالح لفترة محدودة ولا تشاركيه مع أي شخص.</p>
```

ثم من إعدادات Email Provider اجعل مدة OTP قرابة 600 ثانية بما يتوافق مع مدة التفعيل في التطبيق.

## متغيرات البيئة

تأكد من وجود:

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

يمكن استخدام `NEXT_PUBLIC_SUPABASE_ANON_KEY` بدل المفتاح القابل للنشر إذا كان المشروع ما زال يستخدم الاسم القديم.

لا تستخدم بادئة `NEXT_PUBLIC_` مع `SUPABASE_SERVICE_ROLE_KEY`.

## التشغيل والفحص

في CMD داخل المشروع:

```cmd
rmdir /s /q .next
npm run typecheck
npx eslint . --quiet
npm run dev
```

## اختبار المسار

1. أنشئ مؤثرًا تجريبيًا بلا حساب.
2. اربطه بحملة بها تحويل بنكي والدفع بعد النشر.
3. أنشئ رابط الضيف وافتحه في نافذة خاصة.
4. تحقق بآخر 4 أرقام من الجوال.
5. ارفع المحتوى واعتمده من الموظف.
6. أضف رابط النشر.
7. اضغط **تفعيل الحساب عبر البريد**.
8. أدخل بريدًا تجريبيًا وآخر 4 أرقام من الجوال.
9. أدخل OTP المستلم في البريد.
10. أنشئ كلمة مرور جديدة.
11. أضف بيانات بنك تجريبية.
12. افتح `/portal/dashboard` وتحقق من الحملات والمستحقات وملف الأعمال والأداء.
13. من المالية افتح `/dashboard/finance/bank-profiles` واعتمد بيانات البنك.
14. تأكد أن الدفع لا يصبح جاهزًا قبل اعتماد رابط النشر والبنك.

## المؤثر المسجل مسبقًا

إذا كان `influencers.user_id` موجودًا فلن يسمح النظام بإنشاء حساب ثانٍ. تظهر له شاشة تسجيل الدخول، وبعد الدخول ينتقل إلى بيانات البنك المقنّعة لتأكيدها أو طلب تحديثها.

## الحماية

- تفعيل الحساب لا يبدأ إلا من جلسة رابط ضيف تم التحقق منها.
- آخر 4 أرقام يجب أن تطابق جوال المؤثر المرتبط بالتكليف.
- يجب وجود رابط نشر قبل بدء التفعيل.
- OTP محدود بإعادة إرسال كل 60 ثانية وبعدد محاولات إرسال محدود.
- الآيبان والهوية لا يعرضان كاملين للمؤثر داخل الصفحة.
- المنسق والمراجع لا يحصلان على البيانات البنكية الكاملة.
- المؤثر لا يرى حملات أو مستحقات تخص مؤثرًا آخر.
