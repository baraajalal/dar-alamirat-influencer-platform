# Local migration data

Place SmartSuite export files here only when running migration scripts locally.
Do not commit influencer, banking, identity, campaign, or payment data to Git.

Expected local filename for legacy scripts: `data/archive.xlsx`.
